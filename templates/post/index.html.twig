
<style>
    .example-wrapper { margin: 1em auto; max-width: 800px; width: 95%; font: 18px/1.5 sans-serif; }
    .example-wrapper code { background: #F5F5F5; padding: 2px 6px; }
</style>

<div class="container d-flex">

    <div class="col-12 p-0">
        <div class="row">
            <img src="{{ asset(twig_image_dir  ~  posts_folder ~ post.picture ) }}" style="width: 100%">
        </div>
        <div class="row"><h2>{{ post.caption }}</h2></div>
        <div class="row">
            <div class="textarea">{{ post.description }}</div>
        </div>
    </div>

</div>